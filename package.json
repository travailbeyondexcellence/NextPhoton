{
  "name": "next-photon-Zen-EduCare-Management-System",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "workspaces": [
    "client",
    "server",
    "shared/*"
  ],
  "scripts": {
    "start:all": "turbo run dev --filter=client... --filter=server... --parallel --output-logs=full",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "*"
  },
  "dependencies": {
    "zod": "^3.24.4"
  }
}
