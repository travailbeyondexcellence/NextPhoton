{
  "name": "next-photon-Zen-EduCare-Management-System",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "*"
  }
}
