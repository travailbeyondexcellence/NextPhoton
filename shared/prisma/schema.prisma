
// Main Prisma Schema File
// This file contains only the generator and datasource configuration
// All models are organized in separate files in the schemas/ directory

generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models are organized in separate schema files:
// - schemas/auth.prisma - Better-auth models (User, Session, Account, Verification)
// - schemas/roles-permissions.prisma - ABAC system (Role, Permission, UserRole, etc.)
// - schemas/user-profiles.prisma - All role-specific profiles
// - schemas/academic-system.prisma - Curriculum, assessments, subjects
// - schemas/session-management.prisma - Learning sessions and bookings
// - schemas/monitoring-progress.prisma - ECM monitoring and progress tracking
// - schemas/financial-system.prisma - Payments, invoicing, rate management
// - schemas/communication.prisma - Messaging and notifications
// - schemas/analytics-reporting.prisma - Business intelligence and reporting
